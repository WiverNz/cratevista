# Rust dependency licenses

<!--
  Generated by `cargo about generate --workspace about.hbs` (PRD 10, Phase 6.K).
  Deterministic and path/timestamp-free: it lists each accepted SPDX licence and the
  crates that use it, sorted by cargo-about. Do not edit by hand — regenerate and
  commit, or CI's drift check fails. The accepted-licence policy lives in about.toml.
-->

This report covers the **shipped** (non-dev) Rust dependency graph across all four
release targets. Every licence below is in the accepted allowlist in `about.toml`;
CI fails on any dependency outside it.

{{#each licenses}}
## {{name}} ({{id}})

Used by:
{{#each used_by}}
- {{crate.name}} {{crate.version}}
{{/each}}

{{/each}}
