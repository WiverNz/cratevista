# PRD directory

This directory is intentionally initialized without implementation PRDs.

Generate them with Claude Code:

```text
/create-all-prds
```

Or generate one:

```text
/create-prd ISSUES/issue_01_workspace_and_cli.md
```

Implementation must start from an approved PRD, not directly from an issue.
